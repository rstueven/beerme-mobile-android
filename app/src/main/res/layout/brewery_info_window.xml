<?xml version="1.0" encoding="utf-8"?>
<!--
  Custom map-marker bubble. Mirrors osmdroid's bonuspack_bubble (same view IDs,
  so MarkerInfoWindow still auto-populates title/description/subdescription) but
  adds a directions button. Tapping the text body opens the brewery's details
  (wired in MapScreen); tapping the button launches an external navigation app.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/bubble_layout"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:background="@drawable/bonuspack_bubble"
    android:gravity="center_vertical"
    android:orientation="horizontal"><!-- No padding: rely on the 9-patch's content insets so text doesn't
         spill into the bubble's bottom border (as bonuspack_bubble does). -->

    <ImageView
        android:id="@+id/bubble_image"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginEnd="4dp"
        android:visibility="gone"
        android:contentDescription="@null" />

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:orientation="vertical"
        android:paddingStart="4dp"
        android:paddingEnd="4dp">

        <TextView
            android:id="@+id/bubble_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:maxWidth="240dp"
            android:textColor="@android:color/black"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/bubble_description"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:maxWidth="240dp"
            android:textColor="@android:color/black"
            android:textSize="14sp" />

        <TextView
            android:id="@+id/bubble_subdescription"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:maxWidth="240dp"
            android:textColor="#444444"
            android:textSize="12sp" />
    </LinearLayout>

    <ImageButton
        android:id="@+id/bubble_directions"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:background="?android:attr/selectableItemBackgroundBorderless"
        android:contentDescription="@string/directions"
        android:scaleType="centerInside"
        android:src="@drawable/ic_directions" />
</LinearLayout>
